## 2026-04-23 — OpenMeet integration unblocked + UI polish

- **Done:** Resolved #49 (ATProto OAuth scope cache) via client_id rotation to v3. Switched `rpc:net.openmeet.auth` scope to wildcard `aud=*` — the pinned DID form was silently dropped from the consent grant. Fixed OpenMeet `CreateEventDto` (required `maxAttendees`/`categories`/`timeZone`, `location` as string). End-to-end publish confirmed (https://platform.openmeet.net/events/cibc-demo-day-v2-idhgrn). Shipped OG metadata for poll pages (#46) — server intercepts `/p/:did/:rkey` before `express.static` and injects per-poll tags from the PDS record. Added "Unschedule meeting" action with METHOD:CANCEL .ics emails + OpenMeet event deletion; new `openmeetEventSlug` field on the poll lexicon so the slug survives refresh. UI redesign: `.avail-cell--mine` is now a translucent teal wash (no fill, no stripe) so the group heatmap reads underneath; blue per-cell highlight outline replaced by a focused-name filter (hover/click a name, grid shows only their picks). Fixed edit-mode regression (rkey-restore effect now gates on `editing`). Fixed guest refresh (localStorage-restored identity + rkey match). Added `.railwayignore` to stop CLI uploads from overwriting fresh Nixpacks builds with stale local `client/dist`.
- **Decisions:** Kept `hideResponsesUntilSubmit` as a pure data gate (no visual branching). Wildcard `aud=*` over pinned DID — matches OpenMeet's recommended form. Dropped `og:image` + `twitter:card` for now (existing 512×512 asset renders as a giant square on Telegram/Slack); text-only previews ship clean, dynamic per-poll images tracked as #75. Client_id path (`/api/auth/client-metadata-vN.json`) is versioned so future scope changes rotate cleanly. Authored `poll.description` takes precedence over computed dates/times in OG body.
- **State:** Production at avails.zhgnv.com serves v3 client_id + wildcard rpc scope. All primary OpenMeet paths (publish, calendar overlay, unschedule with cleanup) live. GitHub auto-deploys handle pushes; do NOT use `railway up` reactively when a run shows FAILED with only "scheduling build" — that's a cancellation artifact, not a real failure (see `feedback_railway_scheduling_build_is_cancellation.md`).
- **Next:** #75 dynamic per-poll OG images (bigger — needs render stack choice + cache). Several open UI-polish issues (#60 low-severity cleanup, #61 shadcn Button in save/schedule bars, #53/54 mobile touch targets, #69 scheduling-mode transition hint, #72 creator-not-signed-in message). #42 response storage decoupling still open.

## 2026-07-02 — S4: share_poll gated on community membership
- **Done:** `share_poll` now requires the caller to be a verified member of the target community. New `server/src/lib/membership.js` `assertMembership(did, community)` calls community-admin `GET /api/memberships?subject=<did>` (Bearer `CA_CONFIG_SECRET`), fails closed, and is called at the top of `sharePoll` keyed on the verified `authContext.did` (not the poll-owner `did` arg). New env: `CA_MEMBERSHIP_URL`, `CA_CONFIG_SECRET`. 48 tests (added membership.test.js + mcp-share-poll.test.js). Built subagent-driven; per-task + final Opus review (deploy: yes). Deployed. Smoke verified in prod: deny (non-member → "not a member") + allow (`cibc` member → gate passes).
- **Decisions:** Service-to-service lookup (avails already verified the DID via its own OAuth; no CA session to mint a user token from). Gate placed BEFORE resolvePds/config/Telegram so non-members leak nothing. Fail-closed. Trailing-slash guard on `CA_MEMBERSHIP_URL`.
- **State:** Gate live + enforcing at avails.zhgnv.com.
- **Next:** ⚠️ PRE-EXISTING (not S4): scenius-digest `/api/groups` returns `{"groups": {}}` (community-admin `/api/config` HAS the data) → `fetchCommunityGroups()` is empty → `share_poll` (and `list_communities`) fail "Unknown community" for everyone. Fix scenius-digest, or repoint `fetchCommunityGroups` at community-admin `/api/config` directly (community-admin is now the source of truth, S2).

## 2026-07-02 — Repoint community config to community-admin /api/config (fixes the scenius-digest {} bug)
- **Done:** Replaced `fetchCommunityGroups()` (was hitting scenius-digest `/api/groups`, which returned `{"groups": {}}`) to read community-admin `/api/config` directly (reusing `CA_MEMBERSHIP_URL` + `CA_CONFIG_SECRET`, extracting `data.communities`). +regression test `test/mcp-list-communities.test.js` (49 tests). Deployed. Verified in prod: `list_communities` now returns cibc + scenius with topics + channels. `share_poll` unblocked end-to-end (gate + config both live); actual Telegram post still requires explicit user confirmation.
- **Decisions:** community-admin is the config source of truth (S2); scenius-digest was a temporary middleman unrelated to scheduling — dropped from avails' path. `SCENIUS_CONFIG_SECRET` is now unused by avails (left set on Railway, harmless).
- **State:** Live at avails.zhgnv.com; community config loads from community-admin.
- **Next:** none for this thread.

## 2026-07-17 — Standing availability Phase 1 (subagent-driven)
- **Done:** Built the standing-availability epic Phase 1 (Bluesky-list scope): new `chat.avails.scheduling.availability` lexicon (participant-owned PDS), `/api/availability` CRUD, the `/availability` UI (`WeeklyPatternGrid`), OAuth scope + client_id v4, `resolveListAvailability` + `bestCallSlots` (canonical-UTC-grid overlap) + the `schedule_call` MCP tool (no poll; coverage fallback + trust split). 91/91 server tests + client build green. Built subagent-driven (fresh impl + reviewer per task, Opus whole-branch review). **PR #104.**
- **Decisions:** GATE 1 frozen (Artem-confirmed): mint our own NSID; `scope`={type,value}; list-scope publishes **public** to PDS/firehose; `trust` default `confirm`; `validUntil` 8w. `luxon` added as a server dep for DST-safe tz math.
- **State:** PR #104 open (base `main`), NOT merged. Deploy is NOT drop-in — flip Railway `ATPROTO_CLIENT_ID` to the v4 URL **and** deploy together, then `clear-sessions`.
- **Next:** merge + coordinated deploy → run the Task 10 real-protocol smoke (publish ~3 test-DID records on a real Bluesky list, `schedule_call`, verify no poll). Then Phases 2–4 (#4–#6). Fast-follows tracked in #106; pre-existing `generateIcs` bug in #105.

## 2026-07-17 — Phase 1 shipped, smoked, and hardened (same day as the build above)
- **Done:** Merged **#104** — Phase 1 is **live**. Coordinated deploy with **zero sign-in gap**: set `ATPROTO_CLIENT_ID` to v4 with Railway's `skip_deploys` so the running container kept its self-consistent v3 code+env until the merge brought up v4+v4 in one deploy (the architecture.md caveat warns the gap breaks sign-in "either direction" — `skip_deploys` avoids having a direction). Then `clear-sessions` (3 app / 12 oauth). **Task 10 smoke → GATE 1 PASSED**: the frozen lexicon survives a real `putRecord`, verified on Artem's PDS. Smoke also found **#110**. Shipped **#105** (generateIcs positional args), **#108** (CI never ran `npm test` — 0 → 118 tests gated), **#109** (unmatched `/api/*` GETs hung 300s instead of 404), **#110** (schedule_call couldn't see the list owner), **#106** (all six fast-follows), **#116** (requireAuth lazy-restore).
- **Decisions:** **rkey = `sha256(type\nvalue)`** (Artem's call — no third-party records yet), making the duplicate race *unrepresentable* rather than unlikely; kept a **legacy sweep** rather than a delete-by-hand note because the network can't be enumerated (no firehose index) — only 5 DIDs were checkable. **DST accepted, not fixed** (#106 item 4): needs 2am availability on the ~1 transition day/yr, and a fix forces a product question with no clean answer. `schedule_call` includes the list **owner**, derived from the URI authority rather than `data.list.creator.did` — a record can only live in its creator's repo, so it holds without trusting a response shape. MCP limiter 60/hr, matching `responseLimiter` — `/mcp` was the only public entry point missing the existing convention.
- **State:** `main` = `bdf062a`. Live at avails.zhgnv.com, env `ATPROTO_CLIENT_ID` = **v4**. rkey migration **verified on the real record**: one record at `9b10a2d85f79bab62a9e8c2e`, old TID swept, `createdAt` carried across. CI now gates the suite (118) + globs `node --check` over 26 files (was 11 hardcoded, `mcp/` entirely absent — which is why #105 shipped).
- **Next:** **Booking path still UNPROVEN** — `schedule_call` has never returned `booked:true` against real records; needs **one** more member of "Standing Avails Test" to publish (post-#110 Artem counts as one of the two). Hold **Phase 2 (#4)** until then — its trigger builds directly on a path that has never booked. **#117** = startup restore races Railway's health check (cosmetic since #116, but noisy). Client still `npm install` not `npm ci` (#111 follow-up).

## 2026-07-17 — Pre-Phase-2 design pass (same session, after the ship above)
- **Done:** Fixed **#116** from a real prod report ("Cannot read properties of null (reading 'fetchHandler')") — the startup OAuth restore self-fetches its own public URL, which Railway's edge doesn't route to until the deploy is healthy, so it defers with `oauthSession: null` and logs "will retry on demand" — but nothing retried. `requireAuth` now lazy-restores (the pattern `responses.js` and `mcp/handler.js` already used) and 401s cleanly instead of passing null. Filed **#117** (that boot race — a *race*, not a flat failure: a boot that wins logs the health check before the restore). Gave `architecture.md` the **actual client_id rotation recipe** — it warned about the gap but told you to flip the env "in the same push", which is impossible; `skip_deploys` is the answer. Filed the pre-Phase-2 design set: **#118** grid, **#119** voter-scoping, **#120** voting window, **#121** ambient mode, **#122** verify D4, **#123** npm ci; comments on #79/#80 (calendar) and #103 (epic links).
- **Decisions:** **Phase 2's design commitment is "normalize every signal source to a DID"** — NOT "use Bluesky". MC/Telegram/Bluesky are all first-class; likes merely need no identity bridge (`getLikes` → DIDs), so they can *prove* voter-scoping before community-admin#33 lands. That's sequencing, not ranking (Artem corrected an earlier Bluesky-centric framing). **Book for the voters, not the whole list** — the current plan books for the list because a Telegram reaction can't reach a DID without #33. **Option A**: CA creates the proposal post, owns the tally, calls avails with DIDs — avails stays channel-agnostic and needs no new scope; don't *detect* proposals, *create* them. **Signal quality is already solved by three gates** (published a record → liked → `trust: auto`), and `MIN_CALL_COVERAGE` already filters likes from people with no record. **DST accepted, not fixed** (#106 item 4). Ambient deferred, but keep the trigger endpoint proposal-agnostic or it needs surgery later.
- **State:** `main` = `c8dd4cc`. Phase 1 live; everything pushed. **#119's DID-normalization also dissolves D4's dedupe problem** — resolve both sources to a DID and "the same person doing both" becomes a set union, which reframes #33 from an adoption unlock into what makes a cross-channel tally meaningful at all.
- **Next:** **Booking path STILL unproven** — needs one more member of "Standing Avails Test" to publish (post-#110 Artem counts as one of two). **Verify #122** (is `message_reaction` actually attributable?) before Phase 2's threshold design rests on D4 any longer. Then Phase 2 (#4).

## 2026-07-17 — Phase 2 voter-scoped booking + #122 (D4 resolved)
- **Done:** Shipped the first Phase 2 slice (#103/#119): `schedule_call` takes optional `voterDids` and books for exactly those DIDs instead of the whole list. Extracted a shared `resolveAvailabilityForDids` (behaviour-preserving refactor of `resolveListAvailability`), added validation + the resolution branch + voter coverage in the response + schema. Inline TDD, PR #124, merged `b2d372d`, deployed, verified on prod: `voterDids=[real DID]`→1 record, `voterDids=[bogus]`→0 (discriminates new code from old). 126/126. Plan: `docs/plans/2026-07-17-phase2-voter-scoped-booking.md`. Resolved **#122**: D4's "Telegram reactions are anonymous" is FALSE — a config gap (bot subscribed only to `['message']`), not anonymity; verified read-only on the scenius-digest bot (details on #122 + scenius-digest#15).
- **Decisions:** A voter's record scoped to the list IS the opt-in (#110), so D6/D7 needed no new decision — existing `MIN_CALL_COVERAGE` floors apply. `voterDids` is an arg not a new tool (DRY); empty `voterDids` throws (fail-loud, not a silent poll fallback).
- **State:** main=`b2d372d`; Phase 2 leaf live + verified. Open design: #119 (DID-normalization), #120 (window), #121 (ambient). D4 resolution noted on the epic #103.
- **Next:** booking path STILL needs a 2nd real record for `booked:true` (human — one of Patrick/Yuting/Nico/Megan publishes). Then either the CA side of the Bluesky-likes loop (Option A, community-admin) or the Telegram source (scenius-digest#15, now unblocked). Hold the Phase 2 trigger build until a second member publishes.

## 2026-07-20 — Sub-project F: community-feed publish (backend + button)
- **Done:** Publish avails polls to a community's My Community feed. Backend PR #129 (merged+deployed+verified live): `communityFeedPublishedAt` PDS field + index `publishedAt` mirror; shared `publishToCommunityFeed` behind BOTH the `publish_to_community_feed` MCP tool and `POST /api/polls/:did/:rkey/publish-community`; `?published=1` filter; one-time startup backfill; and fixed `publish_to_openmeet` (called undefined `requireAuth` → ReferenceError on every MCP call). PollView Publish/Unpublish button PR #132 (merged+deployed). 152/152 tests. Filed bugs #130 (dead /poll/ email URLs), #131 (lexicon status enum missing 'finalized').
- **Decisions:** One shared fn behind MCP tool + HTTP route (avoided openmeet's duplicated-path smell). PDS record authoritative — index updates only after putRecord succeeds (not best-effort). Backfill index-only + idempotent (undefined-vs-null, no marker) since the server can't write other creators' PDS records.
- **State:** main clean; both PRs merged + deployed; `publish_to_community_feed` verified live on the MCP endpoint.
- **Next:** none for F. Open: #130/#131.
- **Lesson:** Verify plan tooling before writing tests — plan assumed vitest, avails uses node:test; `indexPoll(did,rkey,poll)` is 3 args not one object; persist via `markDirty` not the guessed `schedulePersist`. Architecture held; specifics were guesses.

## 2026-08-02 — Email layer rebuilt; domain + MCP audit
- **Done:** #146 shipped (PR #147): composeEmail() produces HTML+text from plain text, escaping by construction (4 of 5 sites were unescaped, not 2); sendEmail now requires `text` and validates before the no-API-key return. #145 fixed a fallback From pointing at a deleted domain. README MCP line → avails.citizeninfra.org.
- **Decisions:** light-brand email register, not full DESIGN.md treatment (heavier HTML hurts inbox placement). Deep Teal #0f766e for body links — Gather Teal is 3.56:1 and fails AA; exception documented in DESIGN.md.
- **State:** deployed, both hosts 200. avails.citizeninfra.org live but NOT migrated — ATPROTO_CLIENT_ID untouched, so the re-consent cost is unspent.
- **Next:** avails hand-rolls MCP with no SDK and is pinned to protocol 2026-07-21 — decide hand-implement vs adopt the SDK (#142).

## 2026-08-02 — #42 service-repo shipped+verified; community-dropdown 4-layer bug; triage
- **Done:** Fleshed out the Phase 2 design set (#119 DID-normalization, #120 window + `add_to_call`, #121 ambient) + #42/#77 service-repo design; designed community-admin#33 accounts model (lazy accounts, dual-proof linking); filed #125 (triage), #126 (calendar strategy), #127 (private-scope store); closed #47. **Built #42 Stage 1** (service-repo response store, PR #128): responses write to avails' OWN repo via app-password `serviceSession.js`, feature-flagged (`isServiceConfigured()`) so it ships dark; merged + deployed, then Artem provisioned `avails.bsky.social` (`did:plc:3ym2jg7sqlncwowz33rmevjh`) and I **activated + verified in prod** (real-PDS smoke: unauth POST→201 landing in the service repo, DELETE routes there, cleaned up). Fixed the **community-dropdown saga** — 4 stacked bugs, each fix exposing the next: #134→#135 (object-vs-array shape → never rendered), #137 (empty `SelectItem value=""` crash that #135 exposed), #138→#139 (client sent `communityId`, server reads `community` → selection silently dropped), #140 (link-existing: `validatePollUpdate` accepts `community` + `updatePollCommunity` index re-point + a merged Community card in PollView, impeccable-shaped). Triage: closed #72/#76/#33/#125, re-scoped #29 (client tests remain), verdict on #117 (cosmetic).
- **Decisions:** #42 ships dark → merge ≠ activation, provisioning is the switch. Community stored as **id** everywhere (create/#139, listByCommunity, MCP). Merged **one** Community card, not two (DESIGN.md anti-card-proliferation). #33: lazy accounts (no row until a link exists) + dual-proof linking. #33 (avails, UTC slots) closed as superseded by stated-TZ+Luxon.
- **State:** main clean; #42 live + prod-verified (last DoD item met); community feature fully working end-to-end. Open: #136 (private communities in dropdown), #4 (Phase 2 trigger).
- **Next:** Phase 2 trigger (#4) needs the service account to publish availability to the "Standing Avails Test" list (a 2nd record → `booked:true`), OR build #136.
- **Lesson:** Mis-dated the whole session's artifacts (plan file, commits, comments) "2026-07-18", carried from the compaction summary, when it was 2026-08-02 — re-anchor on the system reminder's date after compaction (saved to memory). And: fixing the visible layer of a stacked bug reliably exposes the next one down.

## 2026-08-03 — delete_poll, and why MCP tokens kept "expiring"
- **Done:** `delete_poll` (#148 → #152), exercised in production to remove the #42 smoke poll. MCP DID-binding now persists immediately and both silent auth-rejection paths log which fired (#153). Filed #149 (`schedule_call` books calls and emails people with **no auth**), #150 (domain migration).
- **Decisions:** `delete_poll` allows finalized polls — the REST delete does, and two delete paths disagreeing about what is deletable is worse than either rule. It deliberately leaves service-repo responses alone, matching the REST delete; whether both should sweep them is its own change.
- **State:** main clean, deployed. #149 is the notable open item: verified from a bare curl that anyone can call `schedule_call` against a public list URI and fire ICS invites at its members.
- **Lessons:** (1) **"Token expired" was a lie the client told about a 401.** The JWT was valid for another 22 hours; `extractAuthContext` was rejecting it through a path that returned `null` with no log. Diagnosing one occurrence took Railway deploy logs, an HTTP timeline, a JWT decode and three source files — it should have taken one log line, so both paths now warn. (2) **Asking "what credential do I need?" was the security audit.** Designing CA's caller surfaced that `schedule_call` never opted into the per-tool auth model.
- **Next:** #149 (gate `schedule_call`), #150 (domain), and `add_to_call` from #120 once a real booking exists.

## 2026-08-03 (cont.) — schedule_call gated; one secret was doing three jobs
- **Done:** #149 closed (#155) — `schedule_call` took no credential at all while booking real calls and emailing the people it booked. Now: service secret, or the list's owner (the DID in the `at://` URI *is* the owner, so no lookup). #156 closed across #157 + #158 — `MCP_JWT_SECRET` split off `SESSION_SECRET`, the admin key moved out of a query string into a header with `timingSafeEqual`, and `verifyToken` now checks `iss`/`aud`.
- **Decisions:** Two-deploy ordering — CA shipped the header first, verified SUCCESS, *then* the gate landed; reversed, `fire()` swallows the error and bookings stop with no alert. A **new** secret rather than `CA_CONFIG_SECRET`, because scenius-digest holds that one too. `MCP_ACCEPTED_ISSUERS` is a set, so the #150 domain cutover is a rollover rather than a flag day.
- **State:** All deployed and verified in production across three credential states (anonymous 401, non-owner "Not your list", service 200). 204 tests green. Deliberately did **not** test the allow paths — they book calls and log everyone out.
- **Next:** #150 domain migration, now better specified: `CLIENT_URL` is a second identity anchor, and `icsUidFor` derives ICS UIDs from its host, so a cutover silently breaks cancellation of pre-migration invites.

## 2026-08-04 — domain migration finished; the old host now redirects
- **Done:** #151 (CORS_ORIGINS) merged + verified. Then the whole of #150's OAuth half: `ATPROTO_CLIENT_ID`/`ATPROTO_REDIRECT_URI`/`CLIENT_URL` → `avails.citizeninfra.org`, `MCP_ACCEPTED_ISSUERS` set first so live MCP tokens survived, `CORS_ORIGINS` swapped to the old host. #160 added `legacyHostRedirect` (308, list-valued `LEGACY_HOSTS`), #159 + #161 moved every user-visible string. Pruned 20 stale local branches and 5 merged remote ones.
- **Decisions:** **Move `CLIENT_URL` rather than pin `icsUidFor` to the old host** — the Resend log shows 14 polls invited, **one cancellation ever**, every event already past, so the population the UID protects is empty. `LEGACY_HOSTS` is a list per D-08. 308 not 301, so a stale `POST /mcp` keeps its method. Redirect middleware sits **after** `cors` (browsers don't follow redirects on preflight) and refuses to redirect a host to itself.
- **State:** All deployed and verified — old host 308s with paths preserved, new host 200, metadata self-consistent, jwks 200, CORS correct on all three origins. **No real sign-in round trip has been run** — that needs a browser, and it is also where every user pays the one-time re-consent.
- **Next:** Confirm a real sign-in. Google Search Console + OAuth origins on the final host. Drop the `MCP_ACCEPTED_ISSUERS`/`CORS_ORIGINS` old-host entries on evidence. Decide when `avails.zhgnv.com` stops resolving.

## 2026-08-06 — four fixes shipped, then the calendar design pass
- **Done:** #130 poll links in emails/ICS pointed at `/poll/` while the router serves `/p/` (#162, with a cross-boundary test that reads the client router); #117 dropped the boot-time OAuth restore — the fetcher is bsky.social, not us, so no local ordering fixes it (#163); #164 `GET /mcp` returned the SPA shell instead of 405, which is what makes an MCP client reconnect forever (#165); froze the `.ics` UID namespace so a host move can't orphan invites (#167); CI actions to v7 (#168) and `npm ci` + caching + `node-version-file` (#169, closes #123). Then the #126 calendar design pass: verdicts on all eight issues, #79 closed as already-shipped, #126 closed.
- **Decisions:** avails **will** hold user calendar refresh tokens (#6), v1 `calendar.readonly`, encrypted on the volume — **not** a PDS record, which is world-readable. #126 conflated two questions: where availability comes from (undecided, data arriving) and where a booked event lands (separable; a community calendar reaches everyone with one credential, raised on community-admin#12).
- **State:** all merged and verified in production. #19 is the only unblocked build left in the cluster.
- **Next:** #19 (`meetingUrl` + Jitsi, no credentials needed). #6 is decided but **not urgent** — standing availability only became usable for communities today (#170), so the question it answers has no data yet. Unverified since the host move: a real signed-in round trip (#150).

## 2026-08-06 (cont.) — ca-community scopes, and the window that never parsed
- **Done:** #170 — `ca-community` scopes resolve; scope semantics moved into `server/src/mcp/scope.js`, shared by `tools.js` and `listMembers.js`; `scopeMatches` now compares type AND value; the write validator opened. #174 — a malformed `window` is refused instead of yielding zero slots, which `schedule_call` had been reporting as "no overlapping availability found" and community-admin announcing as the members' own failure. #176 filed: the standing-availability page claims "only people on this list can see the times below", which is false.
- **Decisions:** `ca-community` requires `voterDids` and the service credential — avails cannot enumerate a community's roster, and there is no owner DID in a community id to authorise against. #174 is deliberately strict rather than normalising a date-time: leniency at that boundary is what let the mismatch live, and a rejected booking retries where a wrong one is announced.
- **State:** both merged and verified in production. A real booking succeeded end to end for the first time — two records, correct timezone conversion, `trust: auto` booked and `trust: confirm` returned for confirmation. Committed through a worktree; another agent had in-flight work on `polls.js`/`validate.js`/client, untouched and since landed.
- **Next:** #176 is the honest one — users are being told the opposite of the truth at the moment they publish. `resolveListAvailability` still has no `ca-community` equivalent by design (community-admin#55 would be needed).
