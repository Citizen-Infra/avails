{
  "name": "avails",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "start": "cd server && node src/index.js"
  }
}
